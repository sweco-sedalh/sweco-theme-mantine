module.exports = {
  plugins: {
    'postcss-preset-mantine': {},
    // would like to use this to inline the fonts css, however then the relative paths to the font files are wrong
    // 'postcss-import-url': {}
  }
};
